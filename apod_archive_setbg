#!/bin/bash
path=~/.APOD  # Hardcode instead of using ~ or $HOME if user is unknown, etc
latest=$path/latest

apod_get --archive \
         --path=$path $1

apod_setbg -t $latest/*.txt \
           -i $latest/*.jpg \
	   -o $path/annotated.png \
#	   -s 1920x1080 # Hardcode if unable to autodetect
